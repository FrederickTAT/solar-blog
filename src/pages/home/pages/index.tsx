import { FC } from 'react';
import Button from '@material-ui/core/Button';

const HomePage: FC = () => {
  return (
    <div>
      HomePage
      <Button>Hello</Button>
    </div>
  );
};

export default HomePage;
